# 蚁群算法项目设计文档

## 1. 项目概述

本项目是一个纯 Python 实现的蚁群算法（Ant Colony Optimization, ACO），用于解决旅行商问题（Traveling Salesman Problem, TSP）。项目采用模块化设计，核心代码位于 `aco/` 目录下，包含四个核心文件：`config.py`、`graph.py`、`ant.py`、`colony.py`，以及 `utils/` 目录下的辅助工具和 `examples/` 目录下的测试用例。

---

## 2. 系统架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                    ACO TSP 系统架构                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────┐     ┌─────────────┐     ┌─────────────────┐   │
│  │   config    │     │    graph    │     │    examples     │   │
│  │ (配置参数)   │     │ (图结构)    │     │ (TSP测试实例)   │   │
│  └──────┬──────┘     └──────┬──────┘     └────────┬────────┘   │
│         │                   │                     │            │
│         ▼                   ▼                     ▼            │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                         main                          │   │
│  │                    (主程序入口)                         │   │
│  └─────────────────────────────────────────────────────────┘   │
│                              │                                  │
│                              ▼                                  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                        colony                         │   │
│  │                    (蚁群控制器)                        │   │
│  │  ┌─────────────────────────────────────────────────┐  │   │
│  │  │   ant   ant   ant   ...   ant                   │  │   │
│  │  │ (蚂蚁个体)                                       │  │   │
│  │  └─────────────────────────────────────────────────┘  │   │
│  └─────────────────────────────────────────────────────────┘   │
│                              │                                  │
│                              ▼                                  │
│  ┌─────────────┐     ┌─────────────┐                           │
│  │ math_utils  │     │  visualizer │                           │
│  │ (数学工具)   │     │ (可视化)    │                           │
│  └─────────────┘     └─────────────┘                           │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 3. 类关系图

```
┌─────────────────────────────────────────────────────────────────┐
│                        类关系图                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   ┌──────────────┐        ┌──────────────┐                      │
│   │   ACOConfig  │        │    Graph     │                      │
│   │──────────────│        │──────────────│                      │
│   │ - alpha      │        │ - cities     │                      │
│   │ - beta       │        │ - distance_matrix │                  │
│   │ - rho        │        │ - pheromone_matrix │                 │
│   │ - q          │        │──────────────│                      │
│   │ - num_ants   │        │ + add_city() │                      │
│   │ - max_iter   │        │ + get_distance() │                   │
│   │ - initial_pheromone   │ + initialize_pheromone() │           │
│   └──────┬───────┘        │ + update_pheromone() │              │
│          │                │ + evaporate_pheromone() │           │
│          │                └──────┬───────┘                       │
│          │                       │                               │
│          │                       │                               │
│          ▼                       ▼                               │
│   ┌──────────────────────────────────────────┐                   │
│   │                   Ant                    │                   │
│   │──────────────────────────────────────────│                   │
│   │ - ant_id                                 │                   │
│   │ - graph    ┌──────────────────────┐      │                   │
│   │ - config   │                      │      │                   │
│   │ - path     │                      │      │                   │
│   │ - visited  │                      │      │                   │
│   │ - path_distance                   │      │                   │
│   │ - current_city                     │      │                   │
│   │──────────────────────────────────────────│                   │
│   │ + reset()                              │                   │
│   │ + select_next_city()                   │                   │
│   │ + move_to()                            │                   │
│   │ + complete_tour()                      │                   │
│   │ + get_pheromone_delta()                │                   │
│   │ - _calculate_probabilities()           │                   │
│   │ - _roulette_wheel_selection()          │                   │
│   └──────────────────────────────────────────┘                   │
│                              │                                    │
│                              │ 1..n                              │
│                              ▼                                    │
│   ┌──────────────────────────────────────────┐                   │
│   │                AntColony                 │                   │
│   │──────────────────────────────────────────│                   │
│   │ - graph                                  │                   │
│   │ - config                                 │                   │
│   │ - ants: List[Ant]                        │                   │
│   │ - best_path                              │                   │
│   │ - best_distance                          │                   │
│   │ - history                                │                   │
│   │──────────────────────────────────────────│                   │
│   │ + run()                                  │                   │
│   │ - _run_iteration()                       │                   │
│   │ - _update_pheromone()                    │                   │
│   │ + get_convergence_data()                 │                   │
│   │ + reset()                                │                   │
│   └──────────────────────────────────────────┘                   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 4. 算法流程图

```
┌─────────────────────────────────────────────────────────────────┐
│                    蚁群算法流程图                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    开始                                  │   │
│  └─────────────────────────────────────────────────────────┘   │
│                              ↓                                  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │               初始化参数 (ACOConfig)                     │   │
│  │  ┌─────────────────────────────────────────────────┐  │   │
│  │  │ alpha=1.0, beta=2.0, rho=0.5, Q=100           │  │   │
│  │  │ num_ants=20, max_iter=100                     │  │   │
│  │  └─────────────────────────────────────────────────┘  │   │
│  └─────────────────────────────────────────────────────────┘   │
│                              ↓                                  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │               构建图 (Graph)                             │   │
│  │  ┌─────────────────────────────────────────────────┐  │   │
│  │  │ 读取城市坐标                                   │  │   │
│  │  │ 计算距离矩阵                                   │  │   │
│  │  │ 初始化信息素矩阵 (τ=initial_pheromone)          │  │   │
│  │  └─────────────────────────────────────────────────┘  │   │
│  └─────────────────────────────────────────────────────────┘   │
│                              ↓                                  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │               创建蚁群 (AntColony)                       │   │
│  │  ┌─────────────────────────────────────────────────┐  │   │
│  │  │ 创建 num_ants 个蚂蚁个体                       │  │   │
│  │  │ 初始化 best_path, best_distance                │  │   │
│  │  └─────────────────────────────────────────────────┘  │   │
│  └─────────────────────────────────────────────────────────┘   │
│                              ↓                                  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │          ┌─────── 迭代开始 (max_iter 次) ───────┐      │   │
│  │          │                                      │      │   │
│  │          ↓  ┌─────────────────────────────┐     │      │   │
│  │  重置所有蚂蚁 │  每只蚂蚁随机选择起点      │     │      │   │
│  │          │  └─────────────────────────────┘     │      │   │
│  │          │                                      │      │   │
│  │          ↓  ┌─────────────────────────────┐     │      │   │
│  │  蚂蚁构建路径 │  while 有未访问城市:       │     │      │   │
│  │          │    计算转移概率 P_ij           │     │      │   │
│  │          │    轮盘赌选择下一个城市         │     │      │   │
│  │          │    移动到新城市                │     │      │   │
│  │          │  end while                     │     │      │   │
│  │          │  计算路径总距离                │     │      │   │
│  │          │  └─────────────────────────────┘     │      │   │
│  │          │                                      │      │   │
│  │          ↓  ┌─────────────────────────────┐     │      │   │
│  │  更新最优解 │  if 当前蚂蚁路径更优:        │     │      │   │
│  │          │    更新 best_path              │     │      │   │
│  │          │    更新 best_distance          │     │      │   │
│  │          │  └─────────────────────────────┘     │      │   │
│  │          │                                      │      │   │
│  │          ↓  ┌─────────────────────────────┐     │      │   │
│  │  更新信息素 │  1. 信息素挥发             │     │      │   │
│  │          │     τ_ij = (1-ρ) * τ_ij       │     │      │   │
│  │          │  2. 蚂蚁释放信息素             │     │      │   │
│  │          │     Δτ_ij^k = Q / L_k          │     │      │   │
│  │          │     τ_ij += Δτ_ij^k            │     │      │   │
│  │          │  └─────────────────────────────┘     │      │   │
│  │          │                                      │      │   │
│  │          └──────────────────────────────────────┘      │   │
│  └─────────────────────────────────────────────────────────┘   │
│                              ↓                                  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    输出结果                              │   │
│  │  ┌─────────────────────────────────────────────────┐  │   │
│  │  │ 打印最优路径                                   │  │   │
│  │  │ 打印最优距离                                   │  │   │
│  │  │ 绘制收敛曲线                                   │  │   │
│  │  │ 绘制路径图                                     │  │   │
│  │  └─────────────────────────────────────────────────┘  │   │
│  └─────────────────────────────────────────────────────────┘   │
│                              ↓                                  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    结束                                  │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 5. 核心公式推导

### 5.1 转移概率公式

蚂蚁 k 从城市 i 转移到城市 j 的概率：

```
           [τ_ij(t)]^α * [η_ij]^β
P_ij^k(t) = ───────────────────────────
             Σ_{l∈allowed_k} [τ_il(t)]^α * [η_il]^β
```

其中：
- `τ_ij(t)`: 时刻 t 边 (i,j) 上的信息素浓度
- `η_ij`: 启发式信息，`η_ij = 1/d_ij`，`d_ij` 为城市 i 到 j 的距离
- `α`: 信息素重要程度因子（α ≥ 0）
- `β`: 启发式信息重要程度因子（β ≥ 0）
- `allowed_k`: 蚂蚁 k 尚未访问的城市集合

**推导说明：**
- α 控制信息素的影响，β 控制距离的影响
- 当 α=0 时，算法退化为贪心算法
- 当 β=0 时，算法退化为随机搜索
- 分子表示路径 (i,j) 的吸引力
- 分母是归一化因子，确保概率和为 1

### 5.2 信息素更新公式

#### 5.2.1 信息素挥发

```
τ_ij(t+1) = (1 - ρ) * τ_ij(t)
```

其中：
- `ρ`: 信息素挥发系数（0 ≤ ρ ≤ 1）
- `1-ρ`: 信息素残留因子

#### 5.2.2 信息素沉积

```
τ_ij(t+1) = τ_ij(t) + Σ_{k=1}^{m} Δτ_ij^k(t)
```

其中：
- `m`: 蚂蚁数量
- `Δτ_ij^k(t)`: 第 k 只蚂蚁在本次迭代中释放在边 (i,j) 上的信息素量

#### 5.2.3 蚂蚁系统（AS）信息素沉积规则

```
Δτ_ij^k = { Q / L_k,  如果蚂蚁 k 经过边 (i,j)
          { 0,        否则
```

其中：
- `Q`: 信息素增量常数
- `L_k`: 第 k 只蚂蚁在本次迭代中构建的路径总长度

**设计原理：**
- 路径越短，释放的信息素越多
- 正反馈机制：优质路径会吸引更多蚂蚁选择

### 5.3 伪随机比例规则（可选扩展）

为了增强搜索的确定性，可采用伪随机比例规则：

```
j = { argmax_{l∈allowed_k} [τ_il] * [η_il]^β,  如果 q ≤ q0
    { 按 P_ij^k 概率选择,                        否则
```

其中：
- `q`: [0,1] 区间的随机数
- `q0`: 决策阈值（如 0.9）
- 以 q0 概率选择当前最优边，以 1-q0 概率按转移概率随机选择

---

## 6. 模块说明

### 6.1 `aco/config.py` - 配置参数模块

**核心类：** `ACOConfig`

**功能：** 统一管理蚁群算法的所有参数，提供参数验证功能。

**关键属性：**
| 参数 | 默认值 | 说明 |
|------|--------|------|
| `alpha` | 1.0 | 信息素重要程度因子 |
| `beta` | 2.0 | 启发式信息重要程度因子 |
| `rho` | 0.5 | 信息素挥发系数 |
| `q` | 100.0 | 信息素增量常数 |
| `num_ants` | 20 | 蚂蚁数量 |
| `max_iterations` | 100 | 最大迭代次数 |
| `initial_pheromone` | 1.0 | 初始信息素浓度 |

**关键方法：**
- `_validate()`: 验证参数有效性
- `__repr__()`: 返回配置字符串表示

**设计亮点：**
- 参数集中管理，便于调优
- 内置参数验证，防止非法输入
- 清晰的文档字符串

### 6.2 `aco/graph.py` - 图结构模块

**核心类：** `Graph`

**功能：** 表示 TSP 问题的城市拓扑结构，管理距离矩阵和信息素矩阵。

**关键属性：**
- `cities`: 城市坐标列表 `[(x1,y1), (x2,y2), ...]`
- `distance_matrix`: n×n 距离矩阵
- `pheromone_matrix`: n×n 信息素矩阵

**关键方法：**
| 方法 | 功能 |
|------|------|
| `add_city(x, y)` | 添加城市 |
| `set_cities(cities)` | 设置城市列表 |
| `get_distance(i, j)` | 获取城市 i,j 间距离 |
| `initialize_pheromone(val)` | 初始化信息素矩阵 |
| `update_pheromone(i, j, delta)` | 更新信息素 |
| `evaporate_pheromone(rho)` | 信息素挥发 |
| `calculate_path_distance(path)` | 计算路径总距离 |

**设计亮点：**
- 封装图的所有相关操作
- 信息素矩阵与距离矩阵分离管理
- 静态方法计算欧几里得距离

### 6.3 `aco/ant.py` - 蚂蚁个体模块

**核心类：** `Ant`

**功能：** 模拟单只蚂蚁的行为，包括路径构建、城市选择、信息素计算。

**关键属性：**
- `ant_id`: 蚂蚁编号
- `graph`: 图对象引用
- `config`: 配置对象引用
- `path`: 当前路径 `[city0, city1, ...]`
- `visited`: 已访问城市标记
- `path_distance`: 路径总距离
- `current_city`: 当前所在城市

**关键方法：**
| 方法 | 功能 |
|------|------|
| `reset(start_city)` | 重置蚂蚁状态 |
| `select_next_city()` | 选择下一个城市 |
| `move_to(city)` | 移动到指定城市 |
| `complete_tour()` | 完成一次完整旅行 |
| `get_pheromone_delta()` | 计算信息素增量 |
| `_calculate_probabilities()` | 计算转移概率 |
| `_roulette_wheel_selection()` | 轮盘赌选择 |

**设计亮点：**
- 每只蚂蚁独立维护状态
- 轮盘赌选择实现概率搜索
- 启发式信息与信息素的权衡

### 6.4 `aco/colony.py` - 蚁群控制模块

**核心类：** `AntColony`

**功能：** 蚁群算法的核心控制器，协调所有蚂蚁的行为，执行算法主循环。

**关键属性：**
- `graph`: 图对象
- `config`: 配置对象
- `ants`: 蚂蚁列表 `List[Ant]`
- `best_path`: 全局最优路径
- `best_distance`: 全局最优距离
- `history`: 迭代历史记录 `[(iter, best, avg), ...]`

**关键方法：**
| 方法 | 功能 |
|------|------|
| `run(callback, verbose)` | 运行蚁群算法 |
| `_run_iteration()` | 执行一次迭代 |
| `_update_pheromone()` | 更新信息素 |
| `get_convergence_data()` | 获取收敛数据 |
| `reset()` | 重置蚁群状态 |

**设计亮点：**
- 主循环与个体行为分离
- 回调函数支持进度监控
- 历史记录便于后续分析

### 6.5 `utils/math_utils.py` - 数学工具模块

**核心类：** `MathUtils`

**功能：** 提供通用数学计算工具函数。

**关键方法：**
| 方法 | 功能 |
|------|------|
| `euclidean_distance(p1, p2)` | 计算欧几里得距离 |
| `manhattan_distance(p1, p2)` | 计算曼哈顿距离 |
| `generate_random_cities(n)` | 生成随机城市坐标 |
| `calculate_path_distance(cities, path)` | 计算路径总距离 |
| `normalize(values)` | 归一化列表 |
| `mean(values)` | 计算平均值 |
| `std_dev(values)` | 计算标准差 |

### 6.6 `utils/visualizer.py` - 可视化模块

**核心类：** `Visualizer`

**功能：** 提供 ASCII 风格的可视化输出，便于在终端展示结果。

**关键方法：**
| 方法 | 功能 |
|------|------|
| `draw_convergence_chart()` | 绘制收敛曲线 |
| `draw_path(cities, path)` | 绘制路径图 |
| `print_statistics()` | 打印统计信息 |
| `progress_bar()` | 生成进度条 |

---

## 7. 参数调优建议

### 7.1 参数敏感性分析

#### 7.1.1 α（信息素重要程度因子）

**作用：** 控制信息素在决策中的重要性。

**调优建议：**
- **推荐范围：** [0.5, 5.0]
- **典型值：** 1.0
- **过小（α < 0.5）：** 信息素影响不足，算法接近贪心算法
- **过大（α > 5）：** 过度强调历史经验，易陷入局部最优
- **策略：** 先固定 α=1.0，再根据问题调整

#### 7.1.2 β（启发式信息重要程度因子）

**作用：** 控制距离信息在决策中的重要性。

**调优建议：**
- **推荐范围：** [1.0, 10.0]
- **典型值：** 2.0-5.0
- **过小（β < 1）：** 距离信息被忽视，搜索效率低
- **过大（β > 10）：** 算法过于贪心，易陷入局部最优
- **策略：** 通常 β > α，体现启发式信息的主导作用

#### 7.1.3 ρ（信息素挥发系数）

**作用：** 控制信息素的挥发速度，平衡探索与利用。

**调优建议：**
- **推荐范围：** [0.1, 0.9]
- **典型值：** 0.3-0.5
- **过小（ρ < 0.1）：** 信息素积累过多，早熟收敛
- **过大（ρ > 0.9）：** 信息素挥发过快，遗忘能力过强
- **策略：** 小规模问题用较小 ρ，大规模问题用较大 ρ

#### 7.1.4 Q（信息素增量常数）

**作用：** 控制每只蚂蚁释放的信息素总量。

**调优建议：**
- **推荐范围：** [10, 1000]
- **典型值：** 100
- **影响：** Q 与路径长度的比例决定信息素增量的相对大小
- **策略：** Q ≈ 平均路径长度 × 蚂蚁数量

#### 7.1.5 num_ants（蚂蚁数量）

**作用：** 控制种群规模，影响搜索的多样性。

**调优建议：**
- **推荐范围：** [10, 100]
- **典型值：** 城市数量的 1-2 倍
- **过少：** 搜索多样性不足，易陷入局部最优
- **过多：** 计算成本增加，收敛速度减慢
- **策略：** 小规模问题用 10-20，大规模问题用 50-100

#### 7.1.6 max_iterations（最大迭代次数）

**作用：** 控制算法的停止条件。

**调优建议：**
- **推荐范围：** [50, 500]
- **典型值：** 100-200
- **策略：** 观察收敛曲线，当连续 20-50 次迭代无明显改进时停止

### 7.2 参数组合推荐

#### 7.2.1 标准配置（推荐）

```python
ACOConfig(
    alpha=1.0,
    beta=2.0,
    rho=0.5,
    q=100.0,
    num_ants=max(10, num_cities),
    max_iterations=100
)
```

**适用场景：** 大多数 TSP 问题，平衡探索与利用

#### 7.2.2 探索型配置

```python
ACOConfig(
    alpha=0.5,
    beta=3.0,
    rho=0.3,
    q=100.0,
    num_ants=max(20, num_cities * 2),
    max_iterations=200
)
```

**适用场景：** 复杂多峰问题，需要更多探索

#### 7.2.3 收敛型配置

```python
ACOConfig(
    alpha=2.0,
    beta=3.0,
    rho=0.7,
    q=100.0,
    num_ants=max(10, num_cities),
    max_iterations=100
)
```

**适用场景：** 需要快速收敛，对解的质量要求适中

#### 7.2.4 大规模问题配置

```python
ACOConfig(
    alpha=1.0,
    beta=2.0,
    rho=0.6,
    q=1000.0,
    num_ants=50,
    max_iterations=300
)
```

**适用场景：** 城市数量 > 50 的大规模 TSP 问题

### 7.3 调优策略

#### 7.3.1 网格搜索法

1. 确定每个参数的搜索范围
2. 构建参数网格
3. 对每组参数运行多次实验（3-5 次）
4. 选择平均性能最好的参数组合

**示例：**
```python
alpha_values = [0.5, 1.0, 1.5, 2.0]
beta_values = [1.0, 2.0, 3.0, 5.0]
rho_values = [0.3, 0.5, 0.7]

for alpha in alpha_values:
    for beta in beta_values:
        for rho in rho_values:
            # 运行实验并记录结果
```

#### 7.3.2 单变量调优法

1. 固定其他参数，只调整一个参数
2. 找到该参数的最优值
3. 依次调整所有参数
4. 最后进行整体微调

**顺序建议：** β → α → ρ → num_ants → Q

#### 7.3.3 收敛监控调优

通过观察收敛曲线调整参数：

| 收敛现象 | 可能原因 | 调整建议 |
|----------|----------|----------|
| 收敛太快，解质量差 | α 过大、ρ 过小 | 减小 α，增大 ρ |
| 收敛太慢，波动大 | β 过小、ρ 过大 | 增大 β，减小 ρ |
| 早收敛于局部最优 | 信息素过于集中 | 增加蚂蚁数量，增大 ρ |
| 始终无法收敛 | 参数不平衡 | 调整 α/β 比例 |

#### 7.3.4 性能指标

调优时需关注以下指标：
1. **最优解质量：** 多次运行的最优距离平均值
2. **收敛速度：** 达到最优解所需的迭代次数
3. **稳定性：** 多次运行结果的标准差
4. **运行时间：** 单次迭代的平均时间

### 7.4 常见问题与解决方案

#### 问题1：算法早熟收敛

**现象：** 算法很快收敛，但解的质量很差。

**解决方案：**
- 增大 ρ（信息素挥发系数）
- 增加蚂蚁数量
- 减小 α/β 比值
- 引入信息素最大/最小限制（Max-Min Ant System）

#### 问题2：算法收敛过慢

**现象：** 迭代很多次但没有明显改进。

**解决方案：**
- 增大 β（启发式因子）
- 减小 ρ
- 适当减少蚂蚁数量
- 引入精英策略（Elitist Ant System）

#### 问题3：结果波动太大

**现象：** 多次运行结果差异显著。

**解决方案：**
- 增加迭代次数
- 增加蚂蚁数量
- 调整随机种子
- 引入信息素平滑机制

#### 问题4：计算时间过长

**现象：** 大规模问题运行时间不可接受。

**解决方案：**
- 减少蚂蚁数量（但保持 ≥ 10）
- 减少迭代次数
- 优化距离计算缓存
- 考虑并行化（每只蚂蚁独立计算）

---

## 8. 扩展方向建议

### 8.1 算法改进
1. **精英蚂蚁系统（Elitist AS）：** 最优路径额外释放信息素
2. **最大最小蚂蚁系统（MMAS）：** 限制信息素浓度范围
3. **蚁群系统（ACS）：** 伪随机比例规则 + 局部信息素更新
4. **排序蚂蚁系统（ASrank）：** 按路径长度排序释放信息素

### 8.2 功能扩展
1. **多目标优化：** 支持多目标 TSP
2. **动态 TSP：** 支持城市位置动态变化
3. **并行化：** 利用多进程加速蚂蚁计算
4. **GUI 可视化：** 使用 matplotlib 或 tkinter 实现图形界面

### 8.3 性能优化
1. **距离矩阵预计算：** 目前已实现，可进一步优化缓存
2. **信息素矩阵稀疏化：** 对大规模问题使用稀疏矩阵
3. **NumPy 加速：** 可选使用 NumPy 加速矩阵运算
4. **C 扩展：** 核心计算用 C 语言重写

---

## 9. 总结

本项目实现了一个经典的蚁群算法框架，具有以下特点：

1. **模块化设计：** 四个核心模块职责清晰，易于理解和扩展
2. **纯 Python 实现：** 无外部依赖，便于部署和学习
3. **参数化配置：** 所有关键参数可灵活调整
4. **可视化支持：** ASCII 图表便于快速观察结果
5. **文档完善：** 代码注释完整，设计文档详尽

该框架不仅可用于解决 TSP 问题，还可作为蚁群算法的学习平台，在此基础上进行各种改进和扩展实验。

---

**文档版本：** 1.0  
**创建日期：** 2024年  
**适用版本：** 项目当前版本